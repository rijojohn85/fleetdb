---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: postgrestenants.postgres.rijojohn.xyz
spec:
  group: postgres.rijojohn.xyz
  names:
    kind: PostgresTenant
    listKind: PostgresTenantList
    plural: postgrestenants
    singular: postgrestenant
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PostgresTenant is the Schema for the postgrestenants API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresTenantSpec defines the desired state of PostgresTenant.
            properties:
              databaseName:
                description: |-
                  DatabaseName is the name of the database created for this
                  tenant. It must be a valid Postgres identifies: lowecase letter,
                  digits and underscores, but must not begin with a digit or underscore.
                maxLength: 63
                pattern: ^[a-z][a-z0-9_]*$
                type: string
              postgresVersion:
                default: "16"
                description: PostgresVersion is the Postgres image tag to run.
                type: string
              storageClassName:
                description: |-
                  StorageClassName selects which storageClass to provision the PVC
                  from. If empty, the cluster's default StorageClass will be used
                type: string
              storageSize:
                anyOf:
                - type: integer
                - type: string
                description: |-
                  StorageSize is how much storage to request for the tenant's
                  PersistentVolumeClaim, eg "10Gi"
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
            required:
            - databaseName
            - storageSize
            type: object
          status:
            description: PostgresTenantStatus defines the observed state of PostgresTenant.
            properties:
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recently
                  reconciled generation of this PostgresTenant's Spec
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
